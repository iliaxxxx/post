# CarouselAI - AI-Powered Carousel Generator

Генератор каруселей для соцсетей с использованием Google Gemini AI. Создавайте профессиональные посты для Instagram всего за пару кликов.

## ✨ Возможности

- 🤖 **AI-генерация контента** - автоматическое создание текста на основе темы
- 🎨 **4 готовых темы** - Dark Modern, Minimal Light, Retro Paper, Bold Neon
- 🎭 **5 тональностей** - от экспертного до юмористического
- 📝 **Полная кастомизация** - шрифты, цвета, фон, выравнивание
- 🖼️ **AI-генерация фона** - создание фонового изображения с Gemini
- 📤 **Пакетный экспорт** - скачивание всех слайдов в ZIP
- 📱 **Live Preview** - предпросмотр в iPhone фрейме

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка API ключа

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Добавьте ваш Gemini API ключ:

```
VITE_GEMINI_API_KEY=your_api_key_here
```

Получить API ключ: https://aistudio.google.com/app/apikey

⚠️ **ВАЖНО**: API ключ будет виден в клиентском коде. Для production используйте backend proxy.

### 3. Запуск

```bash
npm run dev
```

Откройте http://localhost:5173

## 📁 Структура проекта

```
post/
├── components/         # React компоненты
│   ├── SlideCard.tsx   # Карточка слайда с темами
│   ├── PhoneFrame.tsx  # iPhone preview frame
│   └── ThemePreview.tsx # Превью темы
├── services/          # API сервисы
│   └── geminiService.ts # Интеграция с Gemini
├── utils/             # Утилиты
│   ├── toneHelpers.ts  # Работа с тональностями
│   └── imageHelpers.ts # Обработка изображений
├── constants.ts       # Константы (цвета, градиенты)
├── types.ts           # TypeScript типы
├── App.tsx            # Главный компонент
└── .env.example       # Пример конфигурации
```

## 🎨 Темы

### Dark Modern
Современный темный дизайн с минималистичным оформлением

### Minimal Light
Светлая минималистичная тема для легкого восприятия

### Retro Paper
Винтажный стиль с бумажной текстурой и рамками

### Bold Neon
Яркая неоновая тема с градиентами

## 🎭 Тональности (Tone of Voice)

- **Экспертный** - профессиональный, лаконичный
- **Заботливый** - мягкий, поддерживающий
- **Вирусный** - краткий, кликбейтный
- **Дерзкий** - провокационный, с триггерами
- **С юмором** - ироничный, легкий

## 🔧 Технологии

- **React 19** + TypeScript
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **Google Gemini AI** - генерация контента и изображений
- **html-to-image** - экспорт в PNG
- **JSZip** - создание архивов

## 📦 Экспорт

Экспорт работает параллельно для всех слайдов:
- Формат: PNG (2.7x pixel ratio)
- Упаковка: ZIP архив
- Именование: `carousel-{тема}.zip`

## ⚡ Оптимизации

### Производительность
- ✅ React.memo для компонентов
- ✅ useCallback для обработчиков
- ✅ useMemo для вычислений
- ✅ Параллельный экспорт слайдов

### Безопасность
- ⚠️ API ключ в клиентском коде (dev only)
- ✅ Валидация данных от API
- ✅ Обработка ошибок
- ✅ .env в .gitignore

### Качество кода
- ✅ TypeScript strict mode
- ✅ Типизированные API ответы
- ✅ Нет использования `any`
- ✅ Константы вынесены в отдельные файлы

## 🔒 Production Deployment

**НЕ ИСПОЛЬЗУЙТЕ** клиентский API ключ в production!

### Рекомендуемая архитектура:

```
Client -> Your Backend API -> Google Gemini
```

### Примеры backend решений:

1. **Express.js**
```javascript
app.post('/api/generate', async (req, res) => {
  const result = await gemini.generate(req.body);
  res.json(result);
});
```

2. **Cloudflare Workers**
3. **Firebase Functions**
4. **Vercel Serverless Functions**

## 🐛 Известные ограничения

1. **API ключ exposed** - требуется backend для production
2. **Нет кэширования** - каждый запрос идет в API
3. **Лимиты Gemini API** - зависят от вашего тарифа

## 📄 Лицензия

MIT

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue.
